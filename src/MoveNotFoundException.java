/**
 * An exception class for when someone makes an invalid move over the network
 */

public class MoveNotFoundException extends Exception {

    /**
     * Empty constructor since the handling of this exception is done
     * elsewhere.
     */
    public MoveNotFoundException() {}
}
